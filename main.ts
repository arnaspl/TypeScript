// Priskiriami tipai kintamiesiems, kad nebutu galima skirtingo tipo kintamajam
const firstName: string = 'Edvardas';
const age: number = 18;
const isMale: boolean = false;

function add(a: number, b: number): number {
    return a + b;
}

add(2, 2);
